/*
Variable - It is a name which is used to store the data.
Rule to implement variable:
1. declare the variable: decide the name of variable and data type of var
   syntax: datatype varname;
   example: int x;
	    char y;
	    float z;
2. Assignment: put value into variable
   syntax: varname=value;
   example:
	    x=10;
	    y='A';
	    z=4.3;

3. Using : apply logic or print the variables values
   syntax: printf("msg + format specifiers",variable names);
	    msg - can be anything
	    format specifiers - %d (int), %c (char), %f (float)
	    variable names: variable name seprated by comma
   example: printf("Value of X=%d",x);
	    printf("Value of Y=%c",y);
	    printf("\nValue of z=%f",z);
*/
#include<stdio.h>
#include<conio.h>
void main()
{
  //declaration
  int x;
  char y;
  float z;

  clrscr();
  //assignment
  x=10;
  y='A';
  z=9.7;
 //using
 printf("\nValue of X=%d",x);
 printf("\nValue of Y=%c",y);
 printf("\nValue of Z=%.2f",z);
 getch();
}