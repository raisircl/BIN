#include<stdio.h>
#include<conio.h>
void main()
{
  float sp, cp, x;
  clrscr();
  printf("\nenter sale price and cost price");
  scanf("%f%f",&sp,&cp);
  if(sp>=cp)
  {
    x=sp-cp;
    printf("\n Profit =%f",x);
  }
  else
  {
    x=cp-sp;
    printf("\nLoss=%f",x);
  }
  getch();
}